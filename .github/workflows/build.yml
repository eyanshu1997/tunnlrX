name: Go Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.22"
      - name: Generate Protobuf
        run: make proto
      - name: Build Server
        run: make server
      - name: Build Client
        run: make client
      - name: Test
        run: go test ./...
